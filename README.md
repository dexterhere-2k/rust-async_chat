A Real time chat server built using Rust and Tokio.


# Getting Started
### Prerequisites
* [Rust & Cargo](https://www.rust-lang.org/tools/install) installed.
* A terminal client like `telnet` or `netcat` (nc).

### 1. Run the Server
Clone the repo and start the server:

```bash
cargo run
